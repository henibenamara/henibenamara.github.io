name: heni_portfolio
description: Flutter Web portfolio - hand-built in Dart, zero third-party packages.
publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true

  # Images / CV live here. Drop your files in and uncomment.
  assets:
    - assets/images/me.png

  # OPTIONAL: bundle real fonts instead of relying on the CDN <link> in web/index.html.
  # Download Space Grotesk + Inter (ttf) into assets/fonts/ then uncomment:
  # fonts:
  #   - family: Space Grotesk
  #     fonts:
  #       - asset: assets/fonts/SpaceGrotesk-Regular.ttf
  #       - asset: assets/fonts/SpaceGrotesk-Medium.ttf
  #         weight: 500
  #       - asset: assets/fonts/SpaceGrotesk-Bold.ttf
  #         weight: 700
  #   - family: Inter
  #     fonts:
  #       - asset: assets/fonts/Inter-Regular.ttf
  #       - asset: assets/fonts/Inter-Medium.ttf
  #         weight: 500
